# Object State Field Pack

Generic object availability and status surface.

## Canonical columns

- `object_active`
- `object_enabled`
- `object_status`

## Boundary

Object state is not workflow. Workflow/lifecycle/process transitions stay in object_workflow.

## Package shape

Each pack has an embeddable, embeddable trait, mirror interface, YAML manifest, registry entry, and quality gate coverage.
